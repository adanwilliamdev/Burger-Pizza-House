export const environment = {
  production: false,
  // Em desenvolvimento, o proxy do Angular CLI (proxy.conf.json) redireciona
  // `/api` para http://localhost:5000, então essa URL relativa é suficiente.
  apiUrl: '/api',
};
