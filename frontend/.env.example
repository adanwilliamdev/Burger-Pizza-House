# URL base da API backend.
# Em desenvolvimento pode ficar vazio (o proxy do Vite em vite.config.ts
# já redireciona /api para http://localhost:5000).
# Em produção, defina a URL real do backend, ex:
# VITE_API_URL=https://api.seurestaurante.com/api
VITE_API_URL=
