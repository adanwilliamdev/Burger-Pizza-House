export const environment = {
  production: true,
  // Em produção, defina a URL real do backend em tempo de build (ex:
  // substituindo este arquivo, ou via `fileReplacements` no angular.json
  // apontando para um arquivo gerado a partir de variáveis de ambiente).
  // Sem isso, o build de produção ficaria preso apontando para localhost.
  apiUrl: 'https://api.seurestaurante.com/api',
};
